# PstOpenOptions

Namespace: `Emcode.Pst.Application`  
Assembly: `Emcode.Pst.Libs`

Opsi pembukaan file PST untuk mengatur mode baca dan validasi.

## Definition

```csharp
public sealed class PstOpenOptions
```

## Properties

- `bool ReadOnly` (default: `true`) - Mode hanya-baca.
- `bool ValidateChecksums` (default: `true`) - Validasi checksum saat membaca.
- `bool AllowAnsi` (default: `true`) - Izinkan PST ANSI.
- `bool AllowUnicode` (default: `true`) - Izinkan PST Unicode.

## Methods

- Tidak ada method publik.

## Events

- Tidak ada event publik.
